# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @nile/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nile-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nile-auth-service && \
          adduser --system -G nile-auth-service nile-auth-service

COPY apps/auth-service/dist nile-auth-service/
COPY apps/auth-service/package.json nile-auth-service/
RUN chown -R nile-auth-service:nile-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nile-auth-service --omit=dev -f install

CMD [ "node", "nile-auth-service" ]
